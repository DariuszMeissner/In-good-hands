import React from 'react'
import './SimpleStepsStepTitle.scss'

export const SimpleStepsStepTitle = ({ title }) => {
    return (
        <span className='fs-5 center'>
            {title}
        </span>
    )
}
